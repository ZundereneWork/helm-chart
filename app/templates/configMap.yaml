{{- range $key, $val := .Values }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{$key}}-config
  labels:
    app: {{$key}}-config
data:
  {{- $val.ConfigMap | toYaml | nindent 2 }}
---
{{- end }} 
